extends base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="Old Password" , name="oldPassword")
        input(placeholder="New Password" , name="newPassword")
        input(placeholder="NEw Password Confirm" , name="newPasswordConfirmation")
        input(value="Change Password", type="submit")